// FIXTURE (negative) — rule engine-file-missing must stay quiet.
//
// Both guarded engine files are present in this tree (this one and
// WhisperKitSpeechRecognizer.swift beside it), so `--require-engine-files`
// is satisfied and the gate must pass.
import Foundation
