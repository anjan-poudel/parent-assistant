// FIXTURE (negative) — rule allow-list-unreadable must stay quiet.
//
// The `--allow-list` points at a readable declaration (LogSanitiserStub.swift
// beside this case), so the key rules have something to judge against.
import Foundation
